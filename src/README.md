# Application source boundary

The existing Next.js `app/` and `components/` directories remain at the repository root.
Formal backend layers are maintained in the root-level `services/`, `repositories/`,
`validators/`, `types/`, `middleware/`, `lib/`, `utils/`, and `config/` directories.
