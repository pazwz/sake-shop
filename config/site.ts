export const SITE_SOCIAL_LINKS = {
  // Add approved customer URLs when they are provided. Empty values render as
  // non-clickable labels so the storefront never links to a guessed account.
  instagram: '',
  xiaohongshu: '',
} as const;
