import Image from 'next/image'; import Link from 'next/link'; import {Product,formatPrice} from '@/lib/products'; import {useLanguage} from './language-provider';
export function ProductCard({product}:{product:Product}){const {categoryLabel,t}=useLanguage();return <Link href={`/products/${product.slug}`} className="group block"><div className="relative aspect-[4/5] overflow-hidden bg-[#29261f]"><Image fill className="object-cover brightness-[.72] sepia-[.18] transition duration-500 group-hover:scale-105 group-hover:brightness-[.9]" src={product.image} alt={product.name}/><div className="pointer-events-none absolute inset-0 bg-[#9b7025]/10 mix-blend-multiply"/></div><p className="mt-4 text-[10px] tracking-[.15em] text-[#b89a60]">{categoryLabel(product.category)}</p><h3 className="mt-1 text-sm">{product.name}</h3><p className="mt-2 text-sm">{formatPrice(product.price)} <span className="text-[10px] text-stone-500">{t('税込','tax incl.','含税','세금 포함')}</span></p></Link>}
