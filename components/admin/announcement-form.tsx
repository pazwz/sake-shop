'use client';
import { useRouter } from 'next/navigation'; import { useState } from 'react';
type Announcement = { id?: string; title?: string; body?: string; type?: string; publishedAt?: Date | string; expiresAt?: Date | string | null; isPublished?: boolean };
export function AnnouncementForm({ announcement }: { announcement?: Announcement }) { const router = useRouter(); const [error, setError] = useState(''); const [busy, setBusy] = useState(false); const [form, setForm] = useState({ title: announcement?.title ?? '', body: announcement?.body ?? '', type: announcement?.type ?? 'GENERAL', publishedAt: announcement?.publishedAt ? new Date(announcement.publishedAt).toISOString().slice(0,16) : new Date().toISOString().slice(0,16), expiresAt: announcement?.expiresAt ? new Date(announcement.expiresAt).toISOString().slice(0,16) : '', isPublished: announcement?.isPublished ?? false }); const save = async (event: React.FormEvent) => { event.preventDefault(); setBusy(true); setError(''); try { const response = await fetch(announcement?.id ? `/api/v1/admin/announcements/${announcement.id}` : '/api/v1/admin/announcements', { method: announcement?.id ? 'PATCH' : 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ ...form, publishedAt: new Date(form.publishedAt).toISOString(), expiresAt: form.expiresAt ? new Date(form.expiresAt).toISOString() : null }) }); if (!response.ok) throw new Error('保存できませんでした。'); router.push('/admin/announcements'); router.refresh(); } catch (reason) { setError(reason instanceof Error ? reason.message : '保存できませんでした。'); } finally { setBusy(false); } }; return <form onSubmit={save} className="mt-8 max-w-2xl space-y-5"><label className="block text-sm">タイトル<input required value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} className="mt-2 w-full border line p-3" /></label><label className="block text-sm">本文<textarea required value={form.body} onChange={(e) => setForm({ ...form, body: e.target.value })} className="mt-2 min-h-48 w-full border line p-3" /></label><label className="block text-sm">種別<select value={form.type} onChange={(e) => setForm({ ...form, type: e.target.value })} className="mt-2 w-full border line p-3"><option value="GENERAL">一般</option><option value="IMPORTANT">重要</option><option value="MAINTENANCE">メンテナンス</option><option value="BUSINESS_HOURS">営業時間</option></select></label><label className="block text-sm">公開日時<input type="datetime-local" required value={form.publishedAt} onChange={(e) => setForm({ ...form, publishedAt: e.target.value })} className="mt-2 w-full border line p-3" /></label><label className="block text-sm">終了日時（任意）<input type="datetime-local" value={form.expiresAt} onChange={(e) => setForm({ ...form, expiresAt: e.target.value })} className="mt-2 w-full border line p-3" /></label><label className="flex gap-2 text-sm"><input type="checkbox" checked={form.isPublished} onChange={(e) => setForm({ ...form, isPublished: e.target.checked })} />公開する</label>{error ? <p className="text-sm text-red-700">{error}</p> : null}<button disabled={busy} className="btn bg-[#171412] text-white">{busy ? '保存中...' : '保存する'}</button></form>; }
