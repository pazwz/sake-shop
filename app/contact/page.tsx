'use client';
import {useSearchParams} from 'next/navigation'; import {Suspense,useState} from 'react'; import {useAuth} from '@/components/auth-provider';
const topics=['商品について','配送について','ご注文前のご相談','注文内容の変更・キャンセル','その他'];
export default function Contact(){return <Suspense fallback={null}><ContactForm/></Suspense>}
function ContactForm(){const params=useSearchParams();const {member}=useAuth();const [sent,setSent]=useState(false);const [topic,setTopic]=useState(params.get('order')?'注文内容の変更・キャンセル':'商品について');const [message,setMessage]=useState('');const order=params.get('order');if(sent)return <div className="wrap flex min-h-[60vh] max-w-2xl flex-col justify-center py-20"><p className="eyebrow">Message received</p><h1 className="serif mt-5 text-5xl">お問い合わせを<br/>承りました。</h1><p className="mt-7 text-sm leading-8 text-stone-600">内容を確認のうえ、通常2営業日以内にメールでご返信します。ご注文の変更・キャンセルは、発送状況によりご希望に添えない場合があります。</p></div>;return <div className="wrap max-w-2xl py-16 md:py-24"><p className="eyebrow">Contact</p><h1 className="serif mt-4 text-5xl">お問い合わせ</h1><p className="mt-6 text-sm leading-7 text-stone-600">商品選びや配送に関するご相談、ご注文後のお問い合わせを承ります。{order&&<><br/>対象のご注文：<span className="font-medium">{order}</span></>}</p><form onSubmit={e=>{e.preventDefault();setSent(true)}} className="mt-10 space-y-6"><label className="block text-xs">お問い合わせ種別<select className="input mt-2" value={topic} onChange={e=>setTopic(e.target.value)}>{topics.map(value=><option key={value}>{value}</option>)}</select></label><label className="block text-xs">メールアドレス<input required type="email" className="input mt-2" defaultValue={member?.email}/></label><label className="block text-xs">お問い合わせ内容<textarea required value={message} onChange={e=>setMessage(e.target.value)} className="mt-2 min-h-40 w-full border border-stone-300 bg-transparent p-3 outline-none focus:border-[#bc9b5d]" placeholder={topic==='注文内容の変更・キャンセル'?'ご希望の内容と理由をご記入ください。':'ご質問・ご相談内容をご記入ください。'}/></label><p className="text-[11px] leading-5 text-stone-500">これはデモフォームです。送信内容は実際にはメール送信されません。</p><button className="btn">送信する</button></form></div>}
