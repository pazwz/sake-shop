'use client';
import Link from 'next/link'; import {useRouter,useSearchParams} from 'next/navigation'; import {Suspense,useState} from 'react'; import {useAuth} from '@/components/auth-provider';
export default function Login(){return <Suspense fallback={null}><LoginForm/></Suspense>}
function LoginForm(){const {login}=useAuth();const router=useRouter();const params=useSearchParams();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const submit=(e:React.FormEvent)=>{e.preventDefault();const result=login(email,password);if(!result.ok){setError(result.message||'ログインできませんでした。');return}router.push(params.get('redirect')||'/mypage')};return <div className="wrap max-w-xl py-16 md:py-24"><p className="eyebrow">Member login</p><h1 className="serif mt-4 text-5xl">ログイン</h1><p className="mt-6 text-sm leading-7 text-stone-600">ご注文履歴や配送状況は、マイページからご確認いただけます。</p><form onSubmit={submit} className="mt-10 space-y-6"><label className="block text-xs">メールアドレス<input required type="email" className="input mt-2" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="block text-xs">パスワード<input required type="password" className="input mt-2" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="text-xs text-red-700">{error}</p>}<button className="btn w-full">ログインする</button></form><div className="mt-10 border-t line pt-7"><p className="text-sm">会員登録がお済みでない方</p><Link href={`/register${params.get('redirect')?`?redirect=${params.get('redirect')}`:''}`} className="btn btn-outline mt-4">新規会員登録</Link></div></div>}
