'use client';
import Link from 'next/link'; import {useRouter,useSearchParams} from 'next/navigation'; import {Suspense,useState} from 'react'; import {useAuth} from '@/components/auth-provider';
export default function Register(){return <Suspense fallback={null}><RegisterForm/></Suspense>}
function RegisterForm(){const {register}=useAuth();const router=useRouter();const params=useSearchParams();const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const submit=(e:React.FormEvent)=>{e.preventDefault();const result=register(name,email,password);if(!result.ok){setError(result.message||'登録できませんでした。');return}router.push(params.get('redirect')||'/mypage')};return <div className="wrap max-w-xl py-16 md:py-24"><p className="eyebrow">Become a member</p><h1 className="serif mt-4 text-5xl">会員登録</h1><p className="mt-6 text-sm leading-7 text-stone-600">KURAの会員になると、ご注文履歴と配送状況をいつでもご確認いただけます。</p><form onSubmit={submit} className="mt-10 space-y-6"><label className="block text-xs">お名前<input required className="input mt-2" value={name} onChange={e=>setName(e.target.value)}/></label><label className="block text-xs">メールアドレス<input required type="email" className="input mt-2" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="block text-xs">パスワード（8文字以上）<input required minLength={8} type="password" className="input mt-2" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="text-xs text-red-700">{error}</p>}<button className="btn w-full">会員登録する</button></form><p className="mt-8 text-xs text-stone-500">これはデモサイトです。登録情報はこのブラウザ内だけに保存されます。</p><p className="mt-5 text-sm">すでに会員の方は <Link href="/login" className="underline">ログイン</Link></p></div>}
